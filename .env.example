# Written by host/install.sh - the numeric group id that owns the helper socket.
AMBERSHELF_GID=

# Interface language when the browser does not ask for one: de or en
AMBERSHELF_LANGUAGE=de

TZ=Europe/Berlin
